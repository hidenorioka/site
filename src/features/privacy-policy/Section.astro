---
type Props = {
  heading: string;
  class?: string;
};

const { heading, class: overrideClass } = Astro.props;
---

<section class:list={[overrideClass]}>
  <h2 class="md:text-[18px] text-[16px] md:mb-6 mb-4">{heading}</h2>
  <slot />
</section>
