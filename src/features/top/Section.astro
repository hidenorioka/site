---
type Props = {
  heading: string;
  class?: string;
};

const { heading, class: overrideClass } = Astro.props;
---

<section class:list={[overrideClass]}>
  <h2 class="font-bold md:text-[20px] text-[18px] md:mb-6 mb-4">{heading}</h2>
  <slot />
</section>
