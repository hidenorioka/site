---
type Props = {
  heading: string;
  id: string;
  class?: string;
};

const { id, heading, class: className } = Astro.props;
---

<section id={id} class:list={[className]}>
  <p class="font-bold md:text-[20px] text-[18px] md:mb-6 mb-4">{heading}</p>
  <slot />
</section>
